from utils import cache

config = cache.Collection('config')
bot_stats = cache.Collection('bot-stats')
whitelist = cache.Collection('whitelist')
nuke_scores = cache.Collection('nuke-scores')
plugins = cache.Collection('plugins')
punishment_entries = cache.Collection('punishment-entries')
punishment_tasks = cache.Collection('punishment-tasks')
